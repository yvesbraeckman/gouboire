import { Component, ChangeDetectionStrategy } from '@angular/core';

@Component({
  selector: 'app-naam',
  changeDetection: ChangeDetectionStrategy.OnPush,
  templateUrl: './naam.html',
  styleUrl: './naam.scss',
})
export class Naam {}
